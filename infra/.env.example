# Copy to infra/.env and adjust before running `pnpm infra:up`.
# Used by docker-compose.dev.yml.

POSTGRES_USER=realtorai
POSTGRES_PASSWORD=change-me-postgres
POSTGRES_DB=realtorai
POSTGRES_PORT=5432

REDIS_PASSWORD=change-me-redis
REDIS_PORT=6379
